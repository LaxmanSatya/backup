constructors
strings

String builder (-sequencal)
String Buffer (=(currently))

== is check 2 references are same memory points
.equals() the 2 variables data is same

string functions
=================

finding length of string use text.length() ::: GivenString.length()

trmiming string: cleaning before and after space in text is removed and space is unicode \u0020 and trim() also lesser than 20 are removes but greater than 20 are Letter like 'A'.

default constructor is implecitly java creates a

and value 0
value null for str

? 
===

if method name is same but return type == jave takes it an error and is not consider as new method :)

if access mofifer only public to main class ? and why 

*** is only raise warning only and it can run successfully but, is  warning ::: this method has a constructor name


***
===


name is not changed but signature is change is called method overloading in polymorphism in java
name is same but actions are different

-

***
===

This keywork : refernec current object

?
===
varibale shaowding ::: why and use ?*** Q

using this to construcor chaning -> this statements is first to implement


[SEE] ::: constructordemo, chaning  string  methods ::: files

[LRN] ::: Encapuslating what is is it and why-